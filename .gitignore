# dependencias
node_modules/
.pnp
.pnp.*

# build
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# entorno — NUNCA commitear secretos (regla 23)
.env
.env.*
!.env.example

# test
coverage/
.vitest/
playwright-report/
test-results/

# editor / sistema
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json
.idea/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# deploy
.vercel

# generado por scripts/db-bundle.mjs — se regenera, no se versiona
supabase/bundle.sql

# generados por `next dev` (Next 16.3+) en cada arranque. No se versionan:
# cada máquina los crea sola, y si estuvieran en el repo el `git pull` de una
# compu que ya los generó fallaría con "untracked files would be overwritten".
AGENTS.md
CLAUDE.md
